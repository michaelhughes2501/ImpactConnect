# CLAUDE.md — artifacts/mockup-sandbox/

Standalone Vite project for design mockups. Has its own `package.json`, `vite.config.ts`, `tsconfig.json`. NOT imported by the main app — don't add it to the main build.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
