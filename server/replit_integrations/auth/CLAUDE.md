# CLAUDE.md — server/replit_integrations/auth/

Replit OIDC auth: passport strategy per hostname, express-session backed by Postgres (`connect-pg-simple`), `isAuthenticated` middleware, and `GET /api/auth/user`.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
