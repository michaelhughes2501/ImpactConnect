# CLAUDE.md — server/routes/

Feature-scoped route modules mounted from `server/index.ts`. Currently: `integrations.ts` (OAuth callbacks + token storage). Dating-feature routes still live one level up in `server/routes.ts`.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
