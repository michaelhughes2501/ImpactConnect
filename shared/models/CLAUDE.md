# CLAUDE.md — shared/models/

Drizzle table definitions for Replit Auth (`sessions`, `auth_users`). These are mandatory for Replit Auth and must be present in the Postgres schema — don't drop them.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
