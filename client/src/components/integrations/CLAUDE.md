# CLAUDE.md — client/src/components/integrations/

IntegrationCard + IntegrationsPanel — the UI for third-party OAuth integrations. Provider config + tokens live in `client/src/lib/integrations/`.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
