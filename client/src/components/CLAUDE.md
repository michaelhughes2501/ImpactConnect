# CLAUDE.md — client/src/components/

App-specific React components (header, bottom-navigation, profile-card, match-modal, safety-banner, sentence-calculator, plus the `export/` and `integrations/` subsystems). UI primitives live in `ui/`.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
