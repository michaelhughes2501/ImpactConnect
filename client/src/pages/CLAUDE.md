# CLAUDE.md — client/src/pages/

Route components. Only some of these are mounted in `App.tsx` at any given time; adding a page means also adding a `<Route>`. Files: landing, home, chat, discover, matches, messages, profile, resources, not-found.

See the repo-root `CLAUDE.md` for stack, layout, the two parallel auth/user concepts, API surface, and conventions. See `replit.md` for the product voice and prison-terminology glossary.
